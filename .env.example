# ============================================
# REQUIRED - Backend will not start without these
# ============================================

# Environment (dev, staging, prod)
ENV=prod

# Database (use Postgres in production: postgresql+psycopg2://user:pass@host:5432/db)
# For Render: Copy from Postgres instance connection string
DATABASE_URL=sqlite:///./linkedin_lead_checker.db

# Security (generate with: openssl rand -hex 32)
# MUST be ≥32 characters in production
JWT_SECRET_KEY=dev-secret-key-change-in-production-at-least-32-chars-long
JWT_ALGORITHM=HS256
JWT_EXPIRE_DAYS=30

# ============================================
# RECOMMENDED - Safe defaults
# ============================================

# OpenAI (disabled by default = $0 cost)
# Set to true ONLY when you have Pro/Team subscribers and ready to spend
OPENAI_ENABLED=false

# CORS (update with your domain)
CORS_ALLOW_ORIGINS=http://localhost,http://localhost:3000,http://127.0.0.1:3000
CORS_ALLOW_ORIGIN_REGEX=chrome-extension://.*

# ============================================
# OPTIONAL - OpenAI (AI Analysis)
# ============================================

# OpenAI API key (only needed if OPENAI_ENABLED=true)
# Get from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-proj-your-openai-key-here

# ============================================
# OPTIONAL - Stripe (Billing)
# ============================================

# Stripe API key (test: sk_test_... or live: sk_live_...)
# Get from: https://dashboard.stripe.com/apikeys
# stripe_api_key=

# Stripe webhook secret (from Stripe dashboard webhooks page)
# stripe_webhook_secret=

# Stripe price IDs (create products in Stripe dashboard)
# stripe_price_pro_id=
# stripe_price_team_id=

# Stripe redirect URLs (adjust for production)
stripe_success_url=http://localhost:3000/billing/success
stripe_cancel_url=http://localhost:3000/billing/cancel

# ============================================
# OPTIONAL - Cost Control (defaults are safe)
# ============================================

# Monthly AI budget contribution per subscriber
# REVENUE_PER_PRO_USER=12.0
# REVENUE_PER_TEAM_USER=36.0

# Estimated AI cost per successful analysis
# AI_COST_PER_ANALYSIS_USD=0.03

# Emergency kill switches
# DISABLE_ALL_ANALYSES=false
# DISABLE_FREE_PLAN=false

# Rate limiting (seconds between analyses)
# RATE_LIMIT_SECONDS=30

# ============================================
# FRONTEND (Next.js) - Not used by backend
# ============================================

# API URL for frontend to call
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Checkout return URL
NEXT_PUBLIC_CHECKOUT_RETURN_URL=http://localhost:3000/checkout-result?session_id={CHECKOUT_SESSION_ID}
